# 功能基底服务器工作区变更总结（2026-09-20）

本次以 195 服务器 `/home/junbo/wyn/codes/CMAT_my` 的实际工作区为准，在服务器创建提交；本机此前的 `07ba71b` 已撤销并保留文件改动。本次不以本机文件覆盖服务器源码。

## 功能范围

- 在已有功能基底 MVP 上补齐真实多 affordance 数据审计、坐标核验、mask 对齐及 PIADv2 精确采样几何索引，分别记录对象身份与类别功能提示配对策略。
- 支持 v1/v2 索引、共享图像池、同一对象共享点采样/增强、有效功能 padding 和 LAS 对象级 batch 展平兼容；检查划分泄漏、词表及功能数约束。
- FBD 仅编码有效提示，支持固定选择器温度；冻结完整 encoder wrapper，并保持冻结骨干为 eval 模式。
- 训练入口支持内部验证集、dataset epoch 更新和正确恢复起始轮次；受控入口恢复 AdamW、保留最佳 checkpoint，并可重新设置后续余弦学习率。
- 提供真实数据验收、固定对象过拟合、提示干预、温度与 K 消融、完整 holdout 分析工具，以及测试和复现实验文档。
- README 中的数据索引及 Stage A 说明采用服务器现有修改。

## 配置边界

保留服务器 `configs/piadv2_fbd.yaml` 原样：该文件虽然名称包含 fbd，但当前 `model.name` 是 `las`，使用 DINOv3、部分图像骨干微调及 Unseen_aff 数据路径。它不是本次受控 Stage A 的配置，不能仅凭文件名当作 FBD 入口。

实际 Stage A 使用 `piadv2_fbd_unseen_obj_stage_a.yaml`；受控比较分别使用 FBD/LAS 的 `holdout_10ep` 和 `holdout_30ep` 配置。本次显式纳入这些被服务器 ignore 规则屏蔽的配置和测试。绝对资源路径保持服务器值，其他环境使用前需调整。

## 实验结论

FBD/LAS 均完成 30 轮，最佳验证 checkpoint 分别为第 29/24 轮。固定前向种子重评 512 个对象、1157 个提示，aIoU 分别为 0.13835/0.16528。FBD 交换提示后降至 0.04329，已能利用提示，但部分对象功能区分幅度不足。两组分割损失定义和可训练参数量不同，下一步优先做损失对照与 union 消融，再补训练 seed；Stage B 关系损失尚未实现。

详见 [30 轮分析](PIADv2_Stage_A_30ep_Analysis.md) 和 [Stage A 记录](PIADv2_Stage_A.md)。

## 提交与验证

- 提交来源为服务器当前源码、配置、测试、README 和功能基底文档；不包含数据、权重、预测、日志及 work 产物。
- 服务器 `.gitignore` 包含其他目录的环境规则，本次保留其未提交状态；下载、缓存脚本和其他无关文件不纳入提交。
- 提交前在服务器 cmat 环境执行 `python -m unittest discover -s tests -v`，并检查暂存区空白错误。结果：50 项测试全部通过；暂存区空白检查通过。
- 本次只整理提交，未启动新训练、未更改损失设计、未使用正式测试集调参，也未推送 Git 托管远端。

## 后续：LAS 同款损失与 union 对照

- FBD 新增可选 LAS 同款分割损失，直接复用 LASLoss；默认保留旧 FBD 目标。受控续训拒绝 loss 配置不一致的 checkpoint。
- 补充 LAS 分割损失 10/30 轮配置、union=0 的实际运行配置快照，以及损失/梯度一致性、padding、union 和续训保护测试。
- 增加三组最佳模型统一诊断工具，校验输入一致性，统计逐功能/类别和对象内区分，生成固定样本的预测及基底图。
- 三组固定验证 aIoU：FBD union=0.2 为 0.14467，union=0 为 0.15082，LAS 为 0.16528。区分不足对象分别为 113/508、94/508、44/508。关闭 union 有部分收益，但仍有提示选择错误和功能退化；单 seed 不支持宣称 FBD 优于 LAS。
- 完整分析见 [union 统一诊断](PIADv2_Stage_A_Union_Comparison.md)。运行日志、权重、预测和图像保留在 work，不提交。
